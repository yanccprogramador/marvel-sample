// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "br.com.yanchristoffer"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
